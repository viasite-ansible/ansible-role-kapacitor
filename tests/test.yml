---
- hosts: all
  tasks:
  - name: Install Packages for Debian
    apt:
      update_cache: yes
      name: '{{item}}'
      state: present
    with_items:
      - apt-transport-https
      - curl
      - net-tools
    when: ansible_os_family == "Debian"

- include: playbook.yml